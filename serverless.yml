---
service: up-settle

frameworkVersion: '2'

provider:
  name: aws
  runtime: nodejs12.x
  lambdaHashingVersion: 20201221

functions:
  transaction:
    handler: index.handleTransaction
    events:
      - http:
          path: /
          method: post
